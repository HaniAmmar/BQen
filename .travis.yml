language: php
sudo: false
group: stable

branches:
  only:
    - main

git:
  depth: 1

php:
  - 7.3
  - 8.0
  - 8.1
  - nightly

matrix:
  allow_failures:
    - php: nightly

script:
  # Compile:
  - phpize
  - ./configure
  - make

  # Run tests:
  - make test
